<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Kamera: yüz ifadesi, el sallama ve kişi tanıma için -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- Mikrofon: sesli sohbet (Speech-to-Text) için -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- İnternet: AI sohbet motoru API çağrıları için (opsiyonel özellik) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="true" />

    <application
        android:label="AI Buddy Kids"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- ML Kit'in yüz/hareket algılama modellerini Play Services
             üzerinden otomatik indirmesi için gerekli meta-data -->
        <meta-data
            android:name="com.google.mlkit.vision.DEPENDENCIES"
            android:value="face,pose" />
    </application>
</manifest>
